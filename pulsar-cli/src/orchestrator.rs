/// Orchestrator stub — future implementation of the Think → Act → Observe loop.
///
/// Will coordinate between the REPL, the viking-context RAG layer, and the
/// inference tier escalation (Tier 1 → Tier 2 → Tier 3).
pub struct Orchestrator;

impl Orchestrator {
    pub fn new() -> Self {
        Self
    }
}
