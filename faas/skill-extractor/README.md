# skill-extractor

`skill-extractor` turns successful resolutions into reusable skill artifacts. It is the learning loop component that helps Pulsar improve repeated work on the same codebase.

The crate builds as both an `rlib` for native tests and a `cdylib` for WebAssembly component packaging.
