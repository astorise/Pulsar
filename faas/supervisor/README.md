# supervisor

`supervisor` decomposes larger work into bounded tasks and tracks worker outcomes. It provides planning helpers for parallel engineering flows.

The crate builds as both an `rlib` for native tests and a `cdylib` for WebAssembly component packaging.
