# viking-context

`viking-context` provides compact workspace context for agents. It indexes files, returns summaries or structural views, and keeps prompts focused on relevant source material.

The crate builds as both an `rlib` for native tests and a `cdylib` for WebAssembly component packaging.
