# Design: Cognitive GC Spec Polish

This is a documentation-only OpenSpec cleanup. The existing requirements for embedding, contradiction clustering, and atomic memory compaction remain unchanged.

The change updates only the main spec Purpose so the capability describes its role in Pulsar's shared-memory lifecycle.
